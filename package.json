{
    "name": "tree-sitter-puppet",
    "version": "1.0.0",
    "description": "",
    "main": "bindings/node",
    "scripts": {
        "test": "tree-sitter generate && tree-sitter test -D",
        "parse": "tree-sitter generate && tree-sitter parse ./test/test.pp",
        "generate": "tree-sitter generate",
        "parse-all": "tree-sitter generate && tree-sitter parse $(find -name '*.pp')"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "nan": "^2.14.2"
    },
    "devDependencies": {
        "tree-sitter-cli": "^0.20.0"
    }
}
